/**
 * Farm plugin build command, build a rust farm plugin
 */
export function build(args: any[]): void {
  // TODO, should automatically install Rust environment automatically
  console.log('call build', args);
}
