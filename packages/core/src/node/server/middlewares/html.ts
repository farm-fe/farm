/**
 * Html middleware used to serve the generated html file.
 * It will read html resources info from the compiler and serve it, Spa fallback is native supported.
 */

export function html() {
  console.log('middleware html');
}
