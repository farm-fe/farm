import { PackageManagerTabs } from "../../../../../../src/theme/PackageManagerTabs";

# @farmfe/plugin-react
支持 `React Jsx` 和 `React Refresh`

## Installation

<PackageManagerTabs command="pnpm add @farmfe/plugin-react" />

## Usage
导入 `@farmfe/plugin-react` 并在 `farm.config.ts` 的 `plugins` 字段中以工厂函数的形式调用，与 JS 插件用法一致。
```ts {2,5}
import { UserConfig } from '@farmfe/core';
import react from '@farmfe/plugin-react';

const config: UserConfig = {
  plugins: [react({ /** options here */ })]
}
```

## Options
请参阅[SWC 转换 React 选项](https://swc.rs/docs/configuration/compilation#jsctransformreact)。
