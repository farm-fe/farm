// const { Project } = require("ts-morph")
// import { Project } from 'ts-morph'
interface obj {
  name: string;
  age: number;
}

const obj: obj = {
  name: 'erkelost',
  age: 18
};

console.log(obj);

// export const project = new Project();

// console.log(project);
