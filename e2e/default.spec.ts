/**
 * Default smoke tests for Farm examples that do not have their own e2e.spec.ts.
 *
 * This file is NO LONGER used directly.  The equivalent logic is now built
 * directly into `scripts/test-e2e.mts` (`runDefaultTest`), which runs as part
 * of the standalone E2E runner (no vitest dependency).
 *
 * To run E2E tests:
 *   pnpm test-e2e
 *   pnpm test-e2e --example react
 *   pnpm test-e2e --from arco-pro
 */
