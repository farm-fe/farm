{
  "name": "{{ project_name }}",
  "version": "1.0.0",
  "scripts": {
    "dev": "farm start",
    "start": "farm start",
    "build": "farm build",
    "preview": "farm preview",
    "clean": "farm clean"{% if biome %},
    "lint": "biome lint",
    "format": "biome format --write"{% endif %}
  },
  "dependencies": {
    "react": "18",
    "react-dom": "18"
  },
  "devDependencies": {{% if biome %}
    "@biomejs/biome": "^1.9.4",{% endif %}
    "@farmfe/cli": "^{{ farm_cli_version }}",
    "@farmfe/core": "^{{ farm_core_version }}",
    "@farmfe/plugin-react": "^1.2.0",
    "@types/react": "18",
    "core-js": "^3.36.1",
    "@types/react-dom": "18",
    "react-refresh": "^0.14.0"
  }
}